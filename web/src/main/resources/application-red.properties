# Perfil «red»: la app escucha en todas las interfaces (aula / Docker).
# Activar: --spring.profiles.active=red
# o FORJAEXAMENES_SERVER_ADDRESS=0.0.0.0 (sin perfil).
#
# AVISO: cambia las contraseñas demo antes de exponer el puerto en la LAN.
server.address=${FORJAEXAMENES_SERVER_ADDRESS:0.0.0.0}
